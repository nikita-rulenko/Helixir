.helix/
target/
*.log
/ansible/
# AI-IDE snapshot artifacts (per-machine tooling, never source)
.snapshots/
/tests/
/scripts/
.helix-archives/
.DS_Store
# Cargo.lock IS committed (helixir/Cargo.lock) — Helixir ships binaries, so a
# locked dep graph = reproducible builds, critical with pinned ort/onnxruntime/
# tokenizers. Do not ignore it. (#11)
__pycache__/

# Private design docs (Moira / generative-memory vision) — not for public exposure yet
*.private.md
helixir/doc/private/

# Runtime agent journals (per-machine activity/insight logs, not source)
helixir-agent-activity.jsonl
helixir-insights.jsonl

# Generated architecture visualization site (local browsing only)
/architecture-site/
